Start-Process powershell -WindowStyle Hidden -ArgumentList '-Command "$wshell = New-Object -ComObject wscript.shell; $end = (Get-Date).Date.AddHours(18).AddMinutes(30); while((Get-Date) -lt $end){$wshell.SendKeys(''{SCROLLLOCK}{SCROLLLOCK}''); Start-Sleep -Seconds (Get-Random -Minimum 30 -Maximum 200)}"'; exit
